apiVersion: v1
kind: Service
metadata:
  name: {{ include "audited.fullname" . }}
  labels:
    {{- include "audited.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "audited.selectorLabels" . | nindent 4 }}
